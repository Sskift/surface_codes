# Core dependencies for both projects
numpy>=1.21.0
matplotlib>=3.5.0

# Dependencies for squeeze.py (quantum spin squeezing simulation)
qutip>=4.7.0
tqdm>=4.64.0

# Optional GPU acceleration for squeeze.py (Linux/Windows only)
# Uncomment the line below if you have CUDA 12.x installed
# cupy-cuda12x>=12.0.0

# For CUDA 11.x, use:
# cupy-cuda11x>=11.0.0

# Note: CuPy installation varies by CUDA version and platform
# See https://docs.cupy.dev/en/stable/install.html for details